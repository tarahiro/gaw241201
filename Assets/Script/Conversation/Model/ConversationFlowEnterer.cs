using Cysharp.Threading.Tasks;
using System;
using System.Collections;
using System.Collections.Generic;
using Tarahiro;
using UniRx;
using UnityEngine;
using VContainer;
using VContainer.Unity;

namespace gaw241201
{
    /*
    public class ConversationFlowEnterer : ICategoryEnterableModel
    {
        [Inject] ConversationModelProvider _provider;

        IConversationModel _model => _provider.MainConversationModel;

        public async UniTask EnterFlow(string bodyId)
        {
            await _model.EnterFlow(bodyId);
        }

        public void ForceEndFlow()
        {
            _model.ForceEndFlow();
        }
    }
    */
}